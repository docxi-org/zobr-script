# ZS MCP Server — copy to .env and adjust
ZS_HOST=127.0.0.1
ZS_PORT=1978
ZS_LIBRARY=./zs-lib
ZS_BUDGET_STEPS=1000
ZS_BUDGET_ITERATIONS=100

ZS_STORE_PATH=./data/store.sqlite

# Logging
LOG_LEVEL=info
# NODE_ENV=production  # uncomment for JSON logs (no pino-pretty)
