# ZS v0.2 — Известные ограничения и отложенные задачи

> Все пункты ждут реального потребителя. Делать когда появится практическая необходимость.

---

## Композиция скриптов (run)

- [x] **Лимит глубины вложенности.** ✅ 2026-06-03
- [ ] **Нет runtime-проверки контракта parent↔child.** Ждёт реального use case с `run()`.
- [ ] **Не тестировалось E2E.** Ждёт реального use case с `run()`.

---

## Конфигурация скриптов (config.json)

- [ ] **config.json per-folder.** `this.config` в srv.ts объявлен, но всегда пустой.
  Ждёт потребителя: 3+ скрипта в одной папке или скрипт с внешними настройками.

---

## Trace hierarchy (parent_seq)

- [ ] **parent_event_seq — причинно-следственные связи в трейсе.**
  Ждёт сложных скриптов с ветвлениями (3+ уровней).

---

## Примеры скриптов

- [ ] **5-8 скриптов-примеров в `zs-lib/examples/`.**
  Пополняется при появлении реальных сценариев, не умозрительно.
