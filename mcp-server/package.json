{
  "name": "zobr-script-mcp",
  "version": "0.1.0",
  "description": "MCP server for ZS (Zobr Script) — cognitive scripting language",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "cf-typegen": "wrangler types",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "agents": "^0.5.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5.9.0",
    "wrangler": "^4.67.0"
  }
}
