---
title: Покрытие
category: Ключевые понятия
order: 3
summary: Покрытие — отношение верифицированного к утверждённому по всем событиям. Одно число, показывающее обоснованность прогона.
tags: [покрытие, доверие, аудит]
related: [trust-classes, trace]
---

# Покрытие

**Покрытие** суммирует весь вызов в одну цифру: из всего, что скрипт сделал, какая доля была [верифицирована](trust-classes) versus просто [утверждена](trust-classes)?

```
verified:  57%  ████████░░░
asserted:  43%  ██████░░░░░
```

## Как считается

Сервер считает события по [классам доверия](trust-classes) в конце каждого прогона:

- **verified** — события с доверием `verified` (контрольные точки, conclude, проверки формы)
- **asserted** — события с доверием `asserted` (когнитивные операции: survey, doubt)
- **authority_gates** — события с доверием `authority` (вызовы серверных функций, I/O)

Покрытие = `verified / (verified + asserted)`. Authority-события считаются отдельно, поскольку представляют внешние зависимости, а не рассуждение модели.

> **Совет:** покрытие — не оценка качества. Короткий полностью верифицированный прогон может быть тривиальным, а богатое asserted-исследование — именно тем, что нужно. Читайте покрытие вместе с [хронологией событий](trace), не отдельно.

## Где вы видите покрытие

- **Обзор** — полоса покрытия на каждой недавней трассировке
- **Список трассировок** — сортируемый столбец
- **Детали трассировки** — круговая диаграмма в панели итогов
- **Детали скрипта → Запуски** — покрытие по каждому вызову

## См. также

- [Классы доверия](trust-classes) — что означают verified и asserted
- [Трассировка](trace) — поток событий, из которого считается покрытие
